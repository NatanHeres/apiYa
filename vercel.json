{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "hooks": {
    "build": "npx playwright install-deps && npm run vercel-build"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}

